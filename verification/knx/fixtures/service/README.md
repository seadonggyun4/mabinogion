# Service Support Fixtures

Static replay cases for unsupported routing and device-management service
classification in the default deterministic lane.

The machine-readable fixture definitions live in `../catalog.toml`.
