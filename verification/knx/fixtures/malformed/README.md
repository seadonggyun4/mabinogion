# Malformed KNXnet/IP Fixtures

Static replay cases for malformed KNXnet/IP headers, body length mismatches, and
unknown service types.

The machine-readable fixture definitions live in `../catalog.toml`.
