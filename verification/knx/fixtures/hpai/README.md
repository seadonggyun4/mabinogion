# HPAI Fixtures

Static replay cases for Host Protocol Address Information validation failures,
including invalid protocol codes and endpoint lengths.

The machine-readable fixture definitions live in `../catalog.toml`.
