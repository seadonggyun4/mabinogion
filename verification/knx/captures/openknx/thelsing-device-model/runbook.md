# OpenKNX/thelsing Device Model Seed

Refresh source:

1. Review OpenKNX/thelsing examples manually for group object and DPT model shape.
2. Convert only stable metadata into `model-reference.json`.
3. Do not vendor firmware, generated ETS database files, or upstream source code.
4. Keep KNX Secure as a future tracking marker unless a separate implementation phase starts.
